// Latency benchmark entry point. W2g and W4c fill this in (median of ten cold starts of the bundled
// `maxims sync --quiet` and of `add --list`, base versus head, plus bundle size).
process.stdout.write("bench: not implemented yet\n");
